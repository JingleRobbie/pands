# Session — 11 Apr 2026, Session 1

**Duration:** ~2 hours  
**Branch:** main

## Shipped / Completed

- **Historical activity rows on the matrix** — past 2 days of PO receipts and confirmed
  production runs appear above "Current Inventory" in gray, with working links, all 5
  fixed columns, delta column, and running balance column
- **Running total per historical row** — baseline fetched via `getBalancesAsOf()` for the
  day before the window; deltas accumulated forward through sorted rows, identical pattern
  to future dated rows in `getMatrixData()`
- **Negative delta format changed globally** — all 4 matrix pages now render negative
  deltas as `(1,800)` in red (`sqft-negative`) instead of `-1,800` with no color; affects
  `matrix/+page.svelte`, `po/[id]/+page.svelte`, `so/[id]/+page.svelte`,
  `production/[id]/confirm/+page.svelte`
- **"Show History" / "Hide History" toggle button** in matrix page header;
  `showHistory = $state(true)` hardcoded on for now, TODO comment to flip later
- **`scripts/seed-history.js`** — idempotent seed script planting 5 POs (JM + Certainteed,
  Apr 2–10) and 8 confirmed production runs across 4 customers (Apr 1–10); explicit
  `created_at` timestamps on all `inventory_transactions` to backdate correctly;
  `npm run seed:history` added to `package.json`
- **`.row-historical` CSS class** in `src/app.css`; odd-row striping selectors updated
  with `:not(.row-historical)` to exclude gray rows from blue alternation

## Decisions Made

- Historical rows query `inventory_transactions` directly (RECEIPT + CONSUMPTION types
  only), not the PO/production tables — the ledger is the authoritative record of what
  actually happened vs. what was planned
- `subType: 'po' | 'production'` field added to distinguish row types within `rowType:
'historical'`; CSS styling is uniformly gray regardless of subType
- `getHistoricalActivityRows()` added as a private helper in `inventory.js`; public
  surface of `getMatrixData()` just gains `historyRows` on the returned object
- `showHistory` defaults to `true` for now — user confirmed they want it always visible
  during development; will flip to `false` and refine toggle UX in a future session

## Bugs & Issues

- **Fixed:** `each_key_duplicate` / `each_key_volatile` Svelte error — HMR had updated
  the component's key expression (`row.subType + row.objectId`) while the browser still
  held old-shape data (no `subType`/`objectId`), producing `NaN` keys; fixed by switching
  to template literal `` `${row.subType}-${row.objectId}` `` and hard-refreshing
- **Fixed:** Historical rows showed same EOD balance for every row on a given date —
  replaced per-date `getBalancesAsOf` snapshot with accumulated running total
- **Fixed:** Historical row columns were collapsed/misaligned — refactored to match the
  full 5-column structure of regular rows

- **Open:** Nothing critical

## Left Half-Done / Weird State

- `showHistory = $state(true)` in `matrix/+page.svelte:37` has a TODO comment — needs
  to flip to `false` once toggle UX is finalized
- All session work (13 files + `scripts/seed-history.js`) is **uncommitted** — needs a
  commit before next session

## Open Questions

- What should the toggle look like long-term? Current "Show History / Hide History" text
  button works but the user mentioned wanting it "on click of something" — icon, row
  header click, or keyboard shortcut?
- Should the history window extend beyond 2 days, or stay at 2? Currently hardcoded as
  `subtractDays(today, 2)` in `getMatrixData()`

## Next Session — Start Here

1. Commit all current work (`git add` the 13 modified files + `scripts/seed-history.js`)
2. Decide and implement the history toggle UX (default hidden, reveal on click)
3. Run `npm run seed:history` if historical rows aren't showing yet

## Context Anchors

- `src/lib/services/inventory.js:171` — `getHistoricalActivityRows()` call in
  `getMatrixData()`; history window is `subtractDays(today, 2)` → 2 days back
- `src/lib/services/inventory.js:350` — `getHistoricalActivityRows()` private function;
  `getBalancesAsOf()` at line ~461 is the helper it relies on for the baseline
- `src/routes/matrix/+page.svelte:37` — `let showHistory = $state(true)` ← change to
  `false` when toggle UX is ready
- `src/routes/matrix/+page.svelte:109` — `{#each matrix.historyRows ...}` block; key is
  `` `${row.subType}-${row.objectId}` ``
- `src/app.css:68` — odd-row striping now excludes both `.row-balance` and
  `.row-historical`; `.row-historical` defined at line ~99

## Notes

- `scripts/seed-history.js` is safe to re-run — all inserts check for existence first
  except `inventory_transactions` which checks by `reference_type + reference_id`
- The negative delta format `(1,800)` is accounting notation — consistent with how
  accountants display debits; positive deltas remain `+1,800` in green
