# Session — 24 Apr 2026, Session 3

**Duration:** ~30 min
**Branch:** main

## Shipped / Completed

- Built a comprehensive UI routing audit reference document covering every route,
  action, status gate, and redirect destination in the app
- Saved to `C:\Users\schel\.claude\plans\have-this-ready-for-warm-lantern.md`
  for automatic load next session
- Organized into 9 domains: Auth, Customers, POs, SOs, WOs, Production,
  Receiving, Calendar, Shipments, Inventory/Matrix, Settings

## Decisions Made

- Walkthrough order agreed: Receiving → Matrix → SO/WO/Production → Shipments
  → Inventory Count → Bulk imports → Admin housekeeping
- Flow map will serve as the live checklist during the UX review session

## Bugs & Issues

- **Fixed:** — none —
- **Open (flagged during mapping):**
  1. `/so/[id]` may have a dead "Schedule" link — agent found a reference to
     `/so/{id}/schedule` but that route file doesn't exist; scheduling goes
     through `/wo/[id]/schedule` instead
  2. `/shipments` list has no "New Shipment" button — only entry is from inside
     `/wo/[id]`; intentional or gap?
  3. `/matrix` has a `+ SO` button — link target needs verification it points
     to `/so/new` and not a stale route

## Left Half-Done / Weird State

- Pending TODO(human) from session 24042026-2 (per memory): finish shipments
  list three-branch empty state + fix broken `$derived(() => ...)` in
  `/shipments/+page.svelte` — not touched this session

## Open Questions

- Are the three flagged routing issues above actual bugs or intentional design?
- Should the walkthrough session be done live in the browser (click every path)
  or is the doc review sufficient?

## Next Session — Start Here

1. **Load the walkthrough plan** — it's at
   `C:\Users\schel\.claude\plans\have-this-ready-for-warm-lantern.md` and
   auto-loads. Start the dev server (`npm run dev`) and walk through each
   domain in order
2. **Resolve the three flagged routing issues** before or during the walkthrough
3. **Pick up the TODO(human)** from session 2 — shipments empty state +
   `$derived` fix in `/shipments/+page.svelte`

## Context Anchors

- Flow map lives in the plan file, not in docs/ — it will auto-load next session
- All 9 domains mapped: Auth, Customers, PO, SO, WO, Production, Receiving,
  Calendar, Shipments, Inventory/Matrix, Settings
- Admin-gated actions: PO cancel, PO import, WO import, run delete, production
  confirm, inventory count, settings/users — all require `role === 'admin'`
- Inventory balance is never stored — always derived from `inventory_transactions`
  ledger (RECEIPT + ADJUSTMENT_IN − CONSUMPTION − ADJUSTMENT_OUT)

## Notes

- No code was written this session — pure audit/documentation work
- 4 commits ahead of origin/main (from previous sessions, not this one)
