# Session — Apr 7, 2026, Session 1

**Branch:** main

## Shipped / Completed

- Migrated the project stack from Python to SvelteKit — full framework switch
- Migrated the database from PostgreSQL to MySQL
- Got the dev environment running end-to-end
- Explored the codebase and started building familiarity with the architecture

## Decisions Made

- **SvelteKit over Python** — Robert is new to Python and prefers to code in JS/Svelte
- **MySQL over PostgreSQL** — MySQL Workbench is comfortable and preferred for day-to-day DB work
- **No TypeScript** — plain JS throughout (already reflected in CLAUDE.md)

## Bugs & Issues

- **Fixed:** `.env` password with a special character (`$`) was being silently eaten by Vite's dotenv-expand. Characters like `$word` are treated as variable references. Fix: escape `$` as `\$` in the `.env` file (documented in CLAUDE.md).
- **Open:** — none —

## Left Half-Done / Weird State

- `CLAUDE.md` has unstaged modifications — likely the `\$` escaping note added this session
- `.claude/commands/` (the `/close-session` command) is untracked and not yet committed

## Open Questions

- — none —

## Next Session — Start Here

1. **Add sample data** — seed realistic inventory transactions so the matrix and balances have something to show
2. Get comfortable running the full app flow: receive a PO line, schedule a production run, confirm it, watch the matrix update

## Context Anchors

- Password escaping gotcha: `DB_PASSWORD` in `.env` must use `\$` not `$` — Vite silently strips `$word` as a variable reference. See CLAUDE.md Environment section.
- Inventory balance is never stored in the DB — always derived by summing `inventory_transactions`. Key file: `src/lib/services/inventory.js`
- The matrix screen (`/matrix`) is the central daily-use screen — start there when showing someone the app

## Notes

First real session on the project. Stack and DB are settled, environment is working. Good foundation for the next round.
