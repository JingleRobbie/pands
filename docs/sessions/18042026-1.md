# Session — 18 Apr 2026, Session 1

**Duration:** ~30 min
**Branch:** main

## Shipped / Completed

- **Production list "Unscheduled" tab fixed** — `src/routes/production/+page.server.js`:
    - Root cause: the existing query started from `production_runs` (INNER JOIN), so SO lines
      with zero runs were invisible
    - Added a dedicated branch for `status === 'unscheduled'` that starts from
      `sales_order_lines` with a LEFT JOIN to `production_runs` — same approach as
      `calendar/[date]/+page.server.js` "Available to Schedule" query
    - Added `JOIN material_skus ms` to get `display_label` and `ms.sort_order`
    - HAVING clause: `sqft_remaining > 0` (alias of the per-line remaining calc)
    - Each SOL row shaped as a pseudo-run object (`id: sol_id`, `display_label`,
      `facing`, `sqft_scheduled: sqft_remaining`, `status: 'UNSCHEDULED'`) so the
      existing Svelte template renders them without modification
    - SOs deduplicated via Map; lines pushed into `so.runs[]` per SO

## Decisions Made

- SOL rows are shaped to match the run object interface rather than branching in the Svelte
  template — keeps the view layer unchanged
- `sqft_remaining` computed as a HAVING alias (cleaner than repeating the expression)
- Sort order: `so.so_number, ms.sort_order` — groups lines by SKU order within each SO

## Bugs & Issues

- **Fixed:** Unscheduled tab showed zero results — query started from wrong table
- **Open:** — none —

## Left Half-Done / Weird State

- `docs/po-update-04-17-26.csv` and `docs/po-update-04-18-26.csv` still untracked — carried
  over from last session; needs a decision (gitignore or delete)
- `TODO(human)` diff display for changed POs in `src/routes/po/import/+page.svelte`
  (~line 85, inside `{:else if po.status === 'changed'}`) — still a blank placeholder

## Open Questions

- Should the two CSV test files in `docs/` be gitignored or deleted?

## Next Session — Start Here

1. Implement the changed-PO diff display in `src/routes/po/import/+page.svelte` — the
   `TODO(human)` block inside `{:else if po.status === 'changed'}`. Each PO has `po.diff`
   with `dateChanged`, `oldDate`, `vendorChanged`, `oldVendor`, `linesAdded`,
   `linesRemoved`, `linesChanged` (each line has `old_sqft`).
2. Test CSV import end-to-end with a real changed PO file.
3. Decide on `received_at` column on `purchase_orders` to fix drifting LEAST(...) date for
   early-received POs in matrix history.
4. Gitignore or delete the two CSV files in `docs/`.

## Context Anchors

- `src/routes/production/+page.server.js:57–91` — new `status === 'unscheduled'` branch;
  early return before the main production_runs query
- `src/routes/calendar/[date]/+page.server.js` — "Available to Schedule" query is the
  pattern the unscheduled fix mirrors
- `src/routes/po/import/+page.svelte:~85` — `TODO(human)` diff display for changed POs;
  `po.diff` shape: `{ dateChanged, oldDate, vendorChanged, oldVendor, linesAdded,
linesRemoved, linesChanged }`

## Notes

- The user applied the initial unscheduled branch themselves; I verified it was correct and
  then extended it to show per-SOL lines inside each SO row
- The adapter pattern (shaping SOL rows to match run object interface) avoided any Svelte
  template changes
