# Session — 16 Apr 2026, Session 1

**Duration:** ~1 hour
**Branch:** main

## Shipped / Completed

- **Calendar events restructured** — `+server.js` now returns `{ [date]: { [entityId]: { type, label } } }`
  instead of a flat array per date. POs collapse to one chip per PO per date (keyed by `po.id`),
  production runs collapse to one chip per SO per date (keyed by `so.id`).
- **Labels updated** — PO: `vendor_name - po_number`; Production: `so_number customer_name - job_name`.
- **Calendar page updated** — event iteration switched from `{#each array}` to
  `{#each Object.entries(obj)}` to match new structure.
- **Edit icon on calendar day cells** — pencil SVG in top-right of each day links to `/calendar/[date]`.
- **Day schedule page scaffolded** — `/calendar/[date]` route created with:
    - Load: fetches runs for the date + all available (unscheduled) SO lines with remaining sqft.
    - `schedule` action: calls existing `scheduleRun()` service.
    - "Scheduled" section: table showing run#, SO, customer, job, SKU, sqft, status badge.
    - "Available to Schedule" section: table skeleton built; row implementation left for user (TODO(human)).
- **Status line configured** — set to display current working directory via `jq` on stdin JSON.

## Decisions Made

- Events keyed by parent entity id (po.id / so.id), not line/run id — deduplicates same-PO or same-SO
  entries that fall on the same date.
- Day schedule page lives at `/calendar/[date]` (sub-route, back button returns to calendar).
- Add-run UX: two-section layout (scheduled on top, available to schedule below with inline sqft input).
- Confirmed runs: editable sqft only (not yet implemented — noted below).

## Bugs & Issues

- **Fixed:** — none —
- **Open:** Available-to-schedule row `TODO(human)` not yet implemented — user was mid-contribution when
  session closed. The existing row code in `+page.svelte` (lines 91–110) is a copy of the scheduled-runs
  row layout and is incorrect — it references `sol.status`, `sol.sqft_actual`, `sol.sqft_scheduled` which
  don't exist on available SO lines. Needs to be replaced with: remaining sqft display + inline
  `?/schedule` form with `so_line_id` hidden input + sqft number input + submit button.

## Left Half-Done / Weird State

- `src/routes/calendar/[date]/+page.server.js` has a `console.log('Available SO Lines:', available)`
  at line 36 (added by user for debugging) — should be removed before shipping.
- The "Available to Schedule" table rows are wrong (see Bugs above).
- Confirmed run editing (sqft_actual) was decided but not implemented — would require adjusting the
  corresponding `inventory_transactions` row, which needs a new service function.

## Open Questions

- Should SCHEDULED runs on the day page be deletable or re-dateable from that view?
- For confirmed run sqft editing: is this needed soon, or can it wait?

## Next Session — Start Here

1. Fix the "Available to Schedule" rows in `src/routes/calendar/[date]/+page.svelte` (lines 91–110):
   replace the broken row with remaining-sqft display + `?/schedule` form per the TODO(human) spec.
2. Remove the `console.log` from `+page.server.js` line 36.
3. Test end-to-end: open `/calendar`, click a day's edit icon, verify both sections render, schedule a run.

## Context Anchors

- `src/routes/calendar/events/+server.js` — events endpoint, now returns `{ date: { id: {type,label} } }`
- `src/routes/calendar/+page.svelte` — calendar grid; edit icon at line ~133; `Object.entries` loop ~140
- `src/routes/calendar/[date]/+page.server.js` — load + `schedule` action; uses `scheduleRun()` from
  `src/lib/services/production.js`
- `src/routes/calendar/[date]/+page.svelte` — day page; "Available" tbody is the broken section
- `src/lib/services/production.js:scheduleRun()` — signature: `(soLineId, date, sqft, userId)`

## Notes

- The available SO lines query uses a `HAVING` clause to filter by remaining sqft
  (`sqft_ordered - sqft_produced - sqft_in_runs > 0`). `sqft_in_runs` is a conditional sum that
  excludes COMPLETED runs, so it counts only active scheduled sqft.
